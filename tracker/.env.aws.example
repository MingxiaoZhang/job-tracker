# AWS Deployment Configuration
# This file shows the environment variables used when running on AWS ECS Fargate
# These are set in the Terraform configuration (aws/terraform/main.tf)
# You don't need to use this file directly - it's just for reference

# Database Configuration
DATABASE_TYPE=dynamodb
DYNAMODB_TABLE_NAME=job-tracker-jobs
AWS_REGION=us-east-1

# Scraper Configuration
SEARCH_QUERY=software engineer
LOCATION=Remote
SCRAPE_INTERVAL_HOURS=6

# Email Notifications (Optional - not yet implemented)
# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587
# SENDER_EMAIL=your-email@gmail.com
# SENDER_PASSWORD=your-app-password
# RECIPIENT_EMAIL=recipient@example.com
# NOTIFY_ON_NEW_JOBS=true
# KEYWORDS_FILTER=python,django,remote
